import "./style.css";

const FormHeader = () =>(
            <fieldset className="header__fieldset">
                <legend className="header__legend">Kalkulator walut</legend>
                </fieldset>
);

export default FormHeader;